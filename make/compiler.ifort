# |
# o---------------------------------------------------------------------o
# |
# | MAD makefile - ifort compiler settings
# |
# o---------------------------------------------------------------------o
# |
# | Methodical Accelerator Design
# |
# | Copyright (c) 2011+ CERN, mad@cern.ch
# |
# | For more information, see http://cern.ch/mad
# |
# o---------------------------------------------------------------------o
# |
# | $Id$
# |

# NOTE: tested on MacOSX and Linux with 32 and 64 bits architecture
# On Linux , libs are statically linked
# On MacOSX, libs are dynamically linked

#
# makedep
#

# fortran dependencies computation requires mod files (quite stupid)!
# FDEP := $(FC) -gen-dep

#
# linker flags
#

LD      := $(FC)
LDFLAGS += -m$(ARCH) -nofor-main
LDLIBS  += -lstdc++

ifeq ($(STATIC),yes)
ifeq ($(OSTYPE),Darwin)
LDFLAGS += -staticlib
else
LDFLAGS += -static
endif
endif

#
# basic flags
#

CPPFLAGS += -D_IFORT
FFLAGS   := -m$(ARCH) -O3 -warn all -warn nounused

# Trig too many warnings for the moment!
# FFLAGS := -m$(ARCH) -O3 -warn all

#
# options flags
#

ifeq ($(DEBUG),yes)
FFLAGS += -g -gdwarf-2
endif

ifeq ($(PROFILE),yes)
FFLAGS  += -pg
LDFLAGS += -pg
endif

#
# extra flags
#

FFLAGS += -fp-model precise -assume noold_unit_star -module $(OBJDIR)

# end of makefile
