# |
# o---------------------------------------------------------------------o
# |
# | MAD makefile - compilers rules
# |
# o---------------------------------------------------------------------o
# |
# | Methodical Accelerator Design
# |
# | Copyright (c) 2011+ CERN, mad@cern.ch
# |
# | For more information, see http://cern.ch/mad
# |
# o---------------------------------------------------------------------o
# |
# | $Id$
# |

vpath %.o $(OBJDIR)

CC_OBJ  := $(patsubst %.c,%.o,$(CC_SRC))
CXX_OBJ := $(patsubst %.cpp,%.o,$(CXX_SRC))
FC_OBJ  := $(patsubst %.F90,%.o,$(patsubst %.f90,%.o,$(FC_SRC)))

HEADERS := $(CC_HDR) $(CXX_HDR)
SOURCES := $(CC_SRC) $(CXX_SRC) $(FC_SRC)
OBJECTS := $(addprefix $(OBJDIR)/,$(CC_OBJ) $(CXX_OBJ) $(FC_OBJ))

#
# rule C compilation
#
$(OBJDIR)/%.o: %.c
	$E "*** Compiling $(<F)"
	$_ $(strip $(CC) $(CFLAGS) $(CPPFLAGS)) -c $< -o $@

#
# rule C++ compilation
#
$(OBJDIR)/%.o: %.cpp
	$E "*** Compiling $(<F)"
	$_ $(strip $(CXX) $(CXXFLAGS) $(CPPFLAGS)) -c $< -o $@

#
# rule f90 compilation
#
$(OBJDIR)/%.o: %.f90
	$E "*** Compiling $(<F)"
	$_ $(strip $(FC) $(FFLAGS)) -c $< -o $@

#
# rule F90 compilation
#
$(OBJDIR)/%.o: %.F90
	$E "*** Compiling $(<F)"
	$_ $(strip $(FC) $(FFLAGS) $(CPPFLAGS)) -c $< -o $@

# end of makefile
