# |
# o---------------------------------------------------------------------o
# |
# | MAD makefile - Libraries settings
# |
# o---------------------------------------------------------------------o
# |
# | Methodical Accelerator Design
# |
# | Copyright (c) 2011+ CERN, mad@cern.ch
# |
# | For more information, see http://cern.ch/mad
# |
# o---------------------------------------------------------------------o
# |
# | $Id$
# |

# ONLINE madx

ifeq ($(ONLINE),yes)
LIBS += -Llib$(ARCH) -lSDDS1c -lSDDS1 -lrpnlib -lmdbmth -lmdblib -lgsl -lz
endif

# Garbage Collector

ifeq ($(USEGC),yes)
ifneq ($(OSTYPE),Windows)
LIBS += -Llibs$/gc -lgc$(ARCH)$(findstring -gnu,$@)
endif
endif

# Libraries on Unixes

ifeq ($(OSTYPE),Linux)
ifeq ($(STATIC),yes)
LIBS += -Llib$(ARCH) 
endif
LDLIBS += -lX11 -lpthread -lstdc++ -lm
endif

ifeq ($(OSTYPE),Darwin)
LIBS += -L/usr/X11/lib -lX11.6
ifeq ($(LD),ifort)
LDLIBS += $(call GNULIB,libgfortran.a) $(call GNULIB,libquadmath.a)
endif
LDLIBS += -lstdc++
endif

# end of makefile
